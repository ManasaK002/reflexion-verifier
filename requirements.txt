jsonlines==3.1.0
tenacity==8.1.0
astunparse==1.6.3
google-genai

# Optional — only needed if you also want to run the GPT4/GPT35 classes
# in generators/model.py for comparison. Not required for the Gemini path.
# openai==0.27.0
